class Helpers {
  static String formatDate(DateTime date) {
    // Customize this date formatting as needed
    return "${date.day}/${date.month}/${date.year}";
  }

// Add more helper functions here as needed
}
